services:
  webdav:
    build: .
    image: local-webdav-server:latest
    container_name: local-webdav-server
    ports:
      - '8080:8080'
    volumes:
      - ./shared:/app/shared
    user: '${UID}:${GID}'
    restart: unless-stopped
